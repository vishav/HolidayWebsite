import { TestBed } from '@angular/core/testing';

import { GregorianKdayafteretcMonthdayService } from './gregorian-kdayafteretc-monthday.service';

describe('GregorianKdayafteretcMonthdayService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: GregorianKdayafteretcMonthdayService = TestBed.get(GregorianKdayafteretcMonthdayService);
    expect(service).toBeTruthy();
  });
});
